# key_logger

#Install dependency.

pip install pynput

pip install threading

pip install smtplib

pip install os

pip install sys

pip install  shutil

pip install subprocess

pip install time

Some module deafult install in your ide,i am use pycharm..!
